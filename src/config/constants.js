export const API = {
  BASE_URL: 'https://nextar.flip.id/frontend-test',
};

export const axiosConfig = {
  headers: {
    'Content-Type': 'application/json',
  },
};

const CONSTANTS = {
  BASE_URL: API.BASE_URL,
  HEADER_JSON: axiosConfig,
};
export default CONSTANTS;
